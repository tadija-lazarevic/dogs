export class Dog {
  constructor(public breed: string, public imageUrl: string, public description: string) {
    this.breed = breed;
    this.imageUrl = imageUrl;
    this.description = description;
  }
}
